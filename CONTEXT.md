# 序拼(XùPīn)

本仓库当前唯一的应用工程:一款 HarmonyOS 本地输入法,核心产品能力是"序列预测"——候选词的生成与排序基于上下文,而不是孤立的静态词频。以下术语是这个应用问题域的语言,不含实现/API 细节。

## Language

**序列预测(Sequence Prediction)**:
输入法的核心能力,统称"候选词的生成与排序基于已经输入/确认的上下文"这件事,包含拼音约束下的[上下文相关排序](#上下文相关排序context-aware-ranking)与不需要拼音输入的[零拼音预测](#零拼音预测zero-pinyin-prediction)两种表现形式。
_Avoid_: 联想输入、AI 预测(过于笼统,不指明是哪种表现形式)

**候选词(Candidate)**:
输入过程中展示给用户、可被选中并上屏的词或字。
_Avoid_: 候选项、建议词

**拼音缓冲区(Composition Buffer)**:
用户已敲入但尚未确认上屏的拼音字符序列,以预览态显示在光标处,尚不构成正式文档内容。
_Avoid_: 预编辑区、输入缓存

**拼音切分(Segmentation)**:
把连续输入的拼音字母串划分为一个或多个合法拼音音节的过程;同一拼音串可能存在多种合法切分路径。
_Avoid_: 分词(分词特指切汉字词,是词典匹配阶段的概念,和这里"切拼音音节"是两个不同阶段,容易混淆)

**上下文相关排序(Context-aware Ranking)**:
结合已确认/已上屏的前文,对某次拼音输入对应的候选词(或候选路径)重新打分排序,而非仅按静态使用频率排序。[序列预测](#序列预测sequence-prediction)在"有拼音输入"场景下的表现形式。
_Avoid_: 智能排序

**零拼音预测(Zero-pinyin Prediction)**:
在拼音缓冲区为空(用户尚未输入任何拼音)的状态下,候选栏依然基于上下文主动给出预测词。[序列预测](#序列预测sequence-prediction)在"无拼音输入"场景下的表现形式,和上下文相关排序共用同一套预测模型,只是候选来源不同(前者从整个词表生成,后者从拼音匹配的候选集合里选)。
_Avoid_: 联想词、下一词预测(容易被误解为独立于拼音转换之外的另一个模块,而不是同一预测模型的另一种候选来源)

**个性化调优(Personalization Tuning)**:
预测模型根据这个用户自己的历史输入在本地持续调整,使排序结果逐渐贴合该用户的用词习惯。和上下文相关排序是两个独立但可叠加的机制——前者看"这句话/这篇文档"的上下文,后者看"这个人"长期的用词历史。
_Avoid_: 个人词库(指传统"用户自造词+频次计数表"的方案,本项目明确要绕开这条路,改成模型层面的调优,两者不是一回事)

**词典数据(Dictionary Data)**:
拼音→字/词的静态映射表,定义"这个拼音可能对应哪些候选词"这个先验集合,可选配套一份粗粒度的静态使用频率。任何拼音输入法(无论有无序列预测能力)都需要这份数据,是[候选词](#候选词candidate)生成阶段的地基,本身不含上下文判断能力。
_Avoid_: 语料库(容易和训练语料混淆——词典给的是词表和粗粒度频率,不是带上下文的句子文本)

**训练语料(Training Corpus)**:
带有真实语序/上下文的成篇中文文本,是[序列预测](#序列预测sequence-prediction)模型(无论统计 n-gram 还是神经模型)据以学习"给定上文,候选词谁更可能"这一判断的数据来源。和[词典数据](#词典数据dictionary-data)是不同性质、不同用途的两类数据,不能互相替代——词典定义候选词全集(骨架),训练语料决定这些候选词在具体语境下谁更可能(判断依据)。
_Avoid_: 词库(和词典数据的常见叫法容易混,注意区分)
